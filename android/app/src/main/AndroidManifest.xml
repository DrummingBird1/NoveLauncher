<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <!-- v9: weather uses only coarse location (WeatherService checks ACCESS_COARSE_LOCATION).
         ACCESS_FINE_LOCATION was declared but never used — removed to avoid over-permissioning
         and the associated Play Console privacy declaration. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.BIND_APPWIDGET" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- v8: CALL_PHONE removed — GlobalSearchScreen uses ACTION_DIAL, which doesn't
         require it. Keeping the permission would have triggered "Privacy & Security"
         warnings in the Play Store listing for no benefit. -->
    <!-- v9: CAMERA removed — there is no Camera-API usage anywhere in the codebase.
         The search bar's optional camera button launches the camera via intent, which
         does NOT require the CAMERA permission. Declaring it was an unused dangerous
         permission (extra attack surface + Play privacy flag). -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- v9.1: scheduled-backup failure notifications (ScheduledBackupWorker). No
         runtime request flow exists yet — on API 33+ without an explicit grant,
         NotificationManagerCompat.notify() is caught and silently skipped, same
         as before this permission existed. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AILauncherApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.AILauncher">

        <!-- v9.3 fix: the Sentry SDK auto-registers a ContentProvider that runs
             BEFORE Application.onCreate() and initializes itself automatically
             unless this is set — it crashes the app on every launch with
             "DSN is required" since no DSN is configured anywhere in this repo
             (see AILauncherApp.initSentry(), which is the only intended init
             path, gated on BuildConfig.SENTRY_DSN being non-blank). -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <activity
            android:name=".ui.LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.AILauncher.Starting"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".ui.screens.SettingsActivity" android:exported="false" />

        <!-- v6: Notification listener for badge counts -->
        <service
            android:name=".data.LauncherNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- v9.1: Quick Settings Tile — "Lock NoveLauncher now" -->
        <service
            android:name=".ui.components.LockLauncherTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher_monochrome"
            android:label="@string/qs_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Widget host -->
        <receiver android:name=".ui.components.LauncherWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info" />
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
